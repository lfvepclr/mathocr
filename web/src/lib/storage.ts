/** localStorage key constants — kept in one place so renames are safe. */
export const STORAGE_KEYS = {
  overlay: 'ocr_overlay',
  viewMode: 'ocr_view_mode',
  splitLeft: 'ocr_split_left',
  sidebarCollapsed: 'sidebarCollapsed',
} as const;
